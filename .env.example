# Provider credentials only. Model IDs and token budgets live in config.toml.
# Empty is fine — Dwar boots without keys. Each route independently returns
# 503 provider_unconfigured naming the missing variable when its own key is unset.

ANTHROPIC_API_KEY=
GEMINI_API_KEY=
OPENAI_API_KEY=
DEEPGRAM_API_KEY=
